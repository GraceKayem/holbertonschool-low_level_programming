#include <stdio.h>
#include "main.h"

/**
 * _abs - Entry point of the program
 *
 * @n: computes the absolute value of an integer.
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
