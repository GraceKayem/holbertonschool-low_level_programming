#include "main.h"
#include <stdio.h>

/**
 * add - Entry point of the program
 *
 * @num1: function that adds two integers and returns the result
 * @num2: function that adds two integers and returns the result
 *
 * Return: Always 0 (Success)
 */
int add(int num1, int num2)
{
return (num1 + num2);
}
